[project]
name = "for-today"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.124.0",
    "pydantic>=2.12.5",
    "pytest>=9.0.2",
    "uvicorn>=0.38.0",
]
